#include <stdio.h>

int main(){

    int a,m;

    printf("Digite um numero: ");
    scanf("%i", &a);

    m = a * 2;
    if ( m > 30)
    {
        printf("multiplicando por 2 o resultado e %i", m);
    }
    
}